import React from 'react'

function Cart() {

    return (
        <div>
            Cart will go here. This component will
            list the Cart items and their quantity before the
            user checks out.
        </div>
    );
}

export default Cart;